[server]
host = "127.0.0.1"
port = 4000

[upstream]
url = "http://localhost:3000"

[prometheus]
enabled = false

[cache]
default_ttl = "5m"
stale_while_revalidate = "1h"
stale_if_error = "24h"

[cache.rules]
"/api/*" = { ttl = "30s", stale = "5m" }
"/static/*" = { ttl = "1d" }
